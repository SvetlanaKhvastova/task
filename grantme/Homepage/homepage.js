let start = setInterval(() => {
  if (document) {
    clearInterval(start);

    let style = /*html */ `
    <style>
        .main_section,
        .counter_section,
        .quote_section,
        .competition-section,
        .future_section,
        .know_exactly_section,
        .know_exactly_section.added_pad,
        .report_card_section,
        .as_seen_section,
        .where_students_section,
        .tuition_section,
        .tips_section,
        .top_universities_section,
        .future_section{
            display: none;
        }
    </style>
    `;
  }
}, 100);
